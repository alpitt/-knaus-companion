# Knaus Companion v4.1.0 deployment

The application files in this package are already arranged as a GitHub Pages site.

Production layout:
- index.html at repository root
- assets/ at repository root
- data/ at repository root
- manual/ at repository root
- .github/workflows/deploy-pages.yml

The included workflow validates the build and publishes the repository contents directly.
