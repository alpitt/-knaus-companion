(function(g){"use strict";const clone=x=>structuredClone(x);function build(events=[]){return clone([...events].sort((a,b)=>String(a.occurredAt||a.recordedAt).localeCompare(String(b.occurredAt||b.recordedAt))||a.id.localeCompare(b.id)))}const api={build,forComponent:(events,id)=>build(events.filter(x=>x.componentIds?.includes(id))),forSystem:(events,id)=>build(events.filter(x=>x.systemIds?.includes(id))),unresolved:events=>clone(events.filter(x=>x.metadata?.status&&/open|pending|unresolved/.test(x.metadata.status)))};g.KnausOwnershipHistory=api;if(typeof module!=='undefined')module.exports=api})(typeof window!=='undefined'?window:globalThis);
