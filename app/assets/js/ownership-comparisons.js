(function(g){"use strict";function period(events=[],from,to){const known=events.filter(x=>x.occurredAt&&x.occurredAt>=from&&x.occurredAt<=to);return{from,to,count:known.length,unknownDateExcluded:events.filter(x=>!x.occurredAt).length,hasData:known.length>0}}function compare(a,b){return{left:a,right:b,difference:a.hasData&&b.hasData?a.count-b.count:null,statement:a.hasData&&b.hasData?'Difference uses recorded samples only.':'Not calculable: missing data is not treated as zero.'}}const api={period,compare,compareDateRanges:(events,a,b)=>compare(period(events,a.from,a.to),period(events,b.from,b.to)),compareSystems:(events,a,b)=>compare({systemId:a,...period(events.filter(x=>x.systemIds?.includes(a)),'','\uffff')},{systemId:b,...period(events.filter(x=>x.systemIds?.includes(b)),'','\uffff')})};g.KnausOwnershipComparisons=api;if(typeof module!=='undefined')module.exports=api})(typeof window!=='undefined'?window:globalThis);
