(function(g){"use strict";let trace=[];function assess(req=[],docs=[],now=new Date()){const available=[],missing=[],expiring=[];for(const r of req){const d=docs.find(x=>x.id===r.documentId||(r.category&&x.category===r.category));if(!d)missing.push(r);else{available.push(d);if(d.expiry){const days=Math.ceil((new Date(d.expiry)-now)/86400000);if(days<=30)expiring.push({...d,days})}}}trace=[{requirements:req.length,available:available.length,missing:missing.length,expiring:expiring.length}];return{required:structuredClone(req),available:structuredClone(available),missing:structuredClone(missing),expiring:structuredClone(expiring),statement:"Requirements are user-defined or template-based; no legal requirement is inferred."}}const api={assess,listRequired:(r,d)=>assess(r,d).required,listAvailable:(r,d)=>assess(r,d).available,listMissing:(r,d)=>assess(r,d).missing,listExpiring:(r,d,n)=>assess(r,d,n).expiring,validateDates:docs=>({valid:docs.every(x=>!x.expiry||!Number.isNaN(Date.parse(x.expiry)))}),getTrace:()=>structuredClone(trace)};g.KnausTripDocuments=api;if(typeof module!=="undefined")module.exports=api})(typeof window!=="undefined"?window:globalThis);
