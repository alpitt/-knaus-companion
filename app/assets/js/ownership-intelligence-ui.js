(function(g){"use strict";let adapter;const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),state=()=>adapter?.getState?.()||{};function render(){g.KnausOwnershipIntelligence?.refreshIndex();const p=g.KnausOwnershipIntelligence?.getOwnershipProfile(),s=g.KnausOwnershipIntelligence?.getSummary()||{},el=$('#ownershipIntelligenceDashboard');if(el)el.innerHTML=`<div class="status-grid"><article class="stat-card"><strong>${esc(p?.title||'Not configured')}</strong><span>Active ownership profile</span></article><article class="stat-card"><strong>${s.total||0}</strong><span>Traceable events</span></article><article class="stat-card"><strong>${s.undated||0}</strong><span>Unknown occurrence dates</span></article><article class="stat-card"><strong>${(state().ownershipCosts||[]).length}</strong><span>Explicit cost records</span></article></div><div class="ownership-actions"><button class="primary-btn" data-route="ownership-timeline">Open Ownership Timeline</button><button class="secondary-btn" data-route="ownership-costs">Analyse costs</button><button class="secondary-btn" data-route="provenance">Explore provenance</button></div><p class="privacy-note">All analysis is local and based only on explicit records. Missing values remain unknown.</p>`}function renderTimeline(){const el=$('#ownershipTimelineResults'),xs=g.KnausOwnershipHistory?.build(g.KnausOwnershipIntelligence?.listEvents()||[])||[];if(el)el.innerHTML=xs.length?xs.map(x=>`<article class="panel ownership-event"><time>${esc(x.occurredAt||'Occurrence date unknown')}</time><h3>${esc(x.title)}</h3><p>${esc(x.summary)}</p><small>Source: ${esc(x.sourceType)} ${esc(x.sourceId)} • recorded ${esc(x.recordedAt)}</small></article>`).join(''):'<div class="panel"><h2>No ownership events</h2><p>Events appear only when an explicit source record exists.</p></div>'}function renderCosts(){const el=$('#ownershipCostLedger'),costs=state().ownershipCosts||[],groups=g.KnausOwnershipTCO?.calculate(costs).byCurrency||{};if(el)el.innerHTML=`<div class="status-grid">${Object.entries(groups).map(([c,v])=>`<article class="stat-card"><strong>${esc(c)} ${Number(v.total.actual).toFixed(2)}</strong><span>Actual • estimated ${Number(v.total.estimated).toFixed(2)} • refunds ${Number(v.total.refunds).toFixed(2)} • reimbursements ${Number(v.total.reimbursements).toFixed(2)}</span></article>`).join('')}</div><div class="record-list">${costs.map(x=>`<article class="panel record-card"><div><span class="meta">${esc(x.category)} • ${esc(x.amountType)}</span><h3>${esc(x.title)}</h3><p>${esc(x.currency)} ${Number(x.amount).toFixed(2)}</p></div></article>`).join('')||'<div class="panel"><h2>No explicit cost records</h2><p>Source modules are never converted into costs automatically.</p></div>'}</div>`}function renderProvenance(){const el=$('#provenanceExplorerResults'),links=state().ownershipProvenanceLinks||[],missing=g.KnausOwnershipProvenance?.getMissingLinks()||[];if(el)el.innerHTML=`<div class="status-grid"><article class="stat-card"><strong>${links.length}</strong><span>Explicit or labelled derived links</span></article><article class="stat-card"><strong>${missing.length}</strong><span>Broken endpoints requiring review</span></article></div>${links.map(x=>`<article class="panel"><h3>${esc(x.fromType)} ${esc(x.fromId)} → ${esc(x.toType)} ${esc(x.toId)}</h3><p>${esc(x.relationshipType)} • ${x.derived?'Derived: '+esc(x.derivationRuleId):x.ownerConfirmed?'Owner confirmed':'Recorded link'}</p></article>`).join('')||'<div class="panel"><p>No provenance links recorded. Links are never fabricated.</p></div>'}`}const api={initialise:o=>(adapter=o.adapter,{status:'ready'}),render,renderTimeline,renderCosts,renderProvenance};g.KnausOwnershipIntelligenceUI=api;if(typeof module!=='undefined')module.exports=api})(typeof window!=='undefined'?window:globalThis);
