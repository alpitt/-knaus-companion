(function(g){"use strict";function calculate(events=[],costs=[]){const dated=events.filter(x=>x.occurredAt),byType=events.reduce((a,x)=>(a[x.eventType]=(a[x.eventType]||0)+1,a),{}),currencies=[...new Set(costs.map(x=>x.currency).filter(Boolean))];return{sampleSize:events.length,datedSampleSize:dated.length,unknownDateCount:events.length-dated.length,eventCounts:byType,costRecordCount:costs.length,currencies,limitations:['Counts describe recorded data only. Missing records are not zero activity.']}}const api={calculate,getUsage:(e)=>calculate(e.filter(x=>x.sourceType==='trip')),getService:(e)=>calculate(e.filter(x=>['maintenance','workshop-job','inspection'].includes(x.sourceType))),getFaults:(e)=>calculate(e.filter(x=>x.sourceType==='fault')),getWorkshops:(e)=>calculate(e.filter(x=>x.sourceType==='workshop-job')),getModifications:(e)=>calculate(e.filter(x=>x.sourceType==='change-plan'))};g.KnausOwnershipStatistics=api;if(typeof module!=='undefined')module.exports=api})(typeof window!=='undefined'?window:globalThis);
