(function(g){"use strict";function analyse(faults=[]){return{sampleSize:faults.length,open:faults.filter(x=>!['resolved','closed','archived'].includes(x.status)).length,resolved:faults.filter(x=>['resolved','closed'].includes(x.status)).length,reliabilityScore:null,statement:'Recorded fault frequency is not a reliability score, diagnosis or proof of root cause.'}}g.KnausOwnershipFaultAnalytics={analyse};if(typeof module!=='undefined')module.exports={analyse}})(typeof window!=='undefined'?window:globalThis);
