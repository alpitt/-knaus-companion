(function(g){"use strict";function analyse(trips=[]){const completed=trips.filter(x=>x.status==='completed');return{sampleSize:trips.length,completed:completed.length,recordedDistance:completed.reduce((s,x)=>s+(Number(x.distance)||0),0),recordedNights:completed.reduce((s,x)=>s+(Number(x.nights)||0),0),locationInference:null,statement:'Uses explicit trip records only; no location is inferred.'}}g.KnausOwnershipTripAnalytics={analyse};if(typeof module!=='undefined')module.exports={analyse}})(typeof window!=='undefined'?window:globalThis);
