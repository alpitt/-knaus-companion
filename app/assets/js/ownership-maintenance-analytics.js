(function(g){"use strict";function analyse(records=[]){return{sampleSize:records.length,completed:records.filter(x=>/completed|verified/.test(x.status||'')).length,deferred:records.filter(x=>/deferred/.test(x.status||'')).length,overdue:records.filter(x=>/overdue/.test(x.dueState||'')).length,statement:'Recorded maintenance activity does not establish overall vehicle condition.'}}g.KnausOwnershipMaintenanceAnalytics={analyse};if(typeof module!=='undefined')module.exports={analyse}})(typeof window!=='undefined'?window:globalThis);
