(function(global){"use strict";let trace=[];const result=(formula,inputs,value,unit,limitations=[])=>{const out={formula,inputs:structuredClone(inputs),result:Number(value.toFixed(3)),unit,rounding:"3 decimal places",limitations};trace=[out];return out},number=(x,name)=>{x=Number(x);if(!Number.isFinite(x))throw Error(`${name} is required and must be numeric.`);return x};
function calculatePower(x){const voltage=number(x.voltage,"Voltage"),current=number(x.current,"Current");return result("P = V × I",x,voltage*current,"W",["Calculated value; not manufacturer approval."])}function calculateCurrent(x){const power=number(x.power,"Power"),voltage=number(x.voltage,"Voltage");if(!voltage)throw Error("Voltage must be greater than zero.");return result("I = P ÷ V",x,power/voltage,"A",["Does not select cable or fuse ratings."])}function calculateEnergy(x){const power=number(x.power,"Power"),hours=number(x.hours,"Time");return result("E = P × t",x,power*hours,"Wh",["Actual consumption depends on duty cycle and losses."])}function calculateVoltageDrop(x){const current=number(x.current,"Current"),length=number(x.length,"Cable length"),resistance=number(x.resistancePerLength,"Resistance per length");return result("Vdrop = I × route length × resistance per length",x,current*length*resistance,"V",["Requires verified conductor data and route-length convention.","Not a cable-size recommendation."])}function calculateWeightChange(x){const added=number(x.added,"Added weight"),removed=number(x.removed,"Removed weight"),factor=x.unit==="lb"?.45359237:1;return result("Net kg = (added − removed) × conversion",x,(added-removed)*factor,"kg",["Does not determine axle or plated-limit compliance."])}function calculatePayloadEffect(x){const remaining=number(x.knownPayload,"Known payload"),change=calculateWeightChange(x).result;return result("Remaining payload = known payload − net added weight",x,remaining-change,"kg",["Valid only when the supplied payload baseline is verified."])}function validateInputs(x,fields){return{valid:fields.every(k=>Number.isFinite(Number(x[k]))),missing:fields.filter(k=>!Number.isFinite(Number(x[k])))}}function getTrace(){return structuredClone(trace)}
const api={calculatePower,calculateCurrent,calculateEnergy,calculateVoltageDrop,calculateWeightChange,calculatePayloadEffect,validateInputs,getTrace};global.KnausChangeCalculations=api;if(typeof module!=="undefined"&&module.exports)module.exports=api;
})(typeof window!=="undefined"?window:globalThis);
