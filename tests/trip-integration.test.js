"use strict";const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path");const root=path.resolve(__dirname,"..");test("trip assets are wired for offline routes and production pages match",()=>{const html=fs.readFileSync(path.join(root,"app/index.html"),"utf8"),fallback=fs.readFileSync(path.join(root,"app/404.html"),"utf8"),worker=fs.readFileSync(path.join(root,"app/service-worker.js"),"utf8");assert.equal(html,fallback);for(const name of["trips.js","trip-readiness.js","trip-checklists.js","trips-ui.js"])assert.match(worker,new RegExp(name.replace(".","\\.")));assert.match(html,/data-screen="trips"/);assert.match(html,/data-screen="trip-dashboard"/);assert.doesNotMatch(worker,/geolocation|gps|weather api/i)});
