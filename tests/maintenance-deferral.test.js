"use strict";const test=require("node:test"),assert=require("node:assert/strict"),d=require("../app/assets/js/maintenance-deferral.js");test("deferral preserves original interval and requires reason",()=>{const m={id:"MP-DEFERITEM1",status:"scheduled",interval:{value:12},defermentHistory:[]},before=JSON.stringify(m.interval);assert.throws(()=>d.defer(m,{}),/reason/i);const x=d.defer(m,{reason:"Awaiting evidence"});assert.equal(JSON.stringify(x.interval),before);assert.match(d.getWarnings(x)[0],/remains/)});
