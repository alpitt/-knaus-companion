"use strict";const test=require("node:test"),assert=require("node:assert/strict");require("../app/assets/js/reasoning-safety.js");const guided=require("../app/assets/js/guided-diagnostics.js");
test("guided flow advances one step and preserves history",()=>{guided.reset();guided.start("electrical power issue");const first=guided.getNextStep();guided.answerStep("Yes");assert.equal(guided.getSession().history.length,1);assert.notEqual(guided.getNextStep().question,first.question)});test("reset clears session",()=>{guided.reset();assert.equal(guided.getSession(),null)});test("safety trigger stops immediately",()=>{guided.start("gas system issue");guided.answerStep("I smell LPG");assert.equal(guided.getSession().status,"safety-stop");assert.ok(guided.getResults().warnings.length)});test("empty final evidence stops as insufficient",()=>{guided.start("fuse or circuit issue");guided.answerStep("circuit");guided.answerStep("");assert.equal(guided.getSession().status,"insufficient-evidence")});test("completed flow provides ranked possibilities without confirmed diagnosis",()=>{guided.start("battery charging issue");guided.answerStep("12.2 V rested");guided.answerStep("leisure battery");const result=guided.getResults();assert.equal(result.possibleCauses[0].rank,1);assert.equal(result.possibleCauses[0].confirmed,false);assert.ok(result.routes.every(x=>x.startsWith("#")))});test("input state is not mutated",()=>{const answer={value:"yes"},before=structuredClone(answer);guided.start("water system issue");guided.answerStep(answer.value);assert.deepEqual(answer,before)});
