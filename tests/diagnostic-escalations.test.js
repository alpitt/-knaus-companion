"use strict";const test=require("node:test"),assert=require("node:assert/strict"),{adapter,fresh}=require("./diagnostic-test-helper");test("escalation acknowledgement retains the escalation",()=>{const a=adapter({diagnosticEscalations:[]}),e=fresh("../app/assets/js/diagnostic-escalations","KnausDiagnosticEscalations");e.initialise(a);assert.throws(()=>e.create({reason:"x",emergencyActionText:"invented"}),/source/);const x=e.create({reason:"configured rule",escalationType:"caution"});assert.equal(e.acknowledge(x.id).status,"acknowledged");assert.equal(e.getActive().length,1)});
