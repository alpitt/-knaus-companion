"use strict";const test=require("node:test"),assert=require("node:assert/strict"),p=require("../app/assets/js/trip-energy-plan.js");test("energy plan uses entered capacity, voltage, fraction and loads",()=>{assert.equal(p.calculateAvailableEnergy({capacityAh:100,nominalVoltage:12,usableFraction:.5}),600);assert.equal(p.calculateExpectedConsumption({loads:[{watts:20,hours:3}]}),60);assert.equal(p.calculateAvailableEnergy({capacityAh:100,nominalVoltage:12}),null)});
