const test=require("node:test"),assert=require("node:assert/strict"),api=require("../app/assets/js/evidence-file-validation.js");
const sig={"image/jpeg":[255,216,255],"image/png":[137,80,78,71,13,10,26,10],"image/webp":[82,73,70,70,0,0,0,0,87,69,66,80],"application/pdf":[37,80,68,70,45],"text/plain":[104,105]};
test("allowed signatures and extensions validate",()=>{for(const[type,b]of Object.entries(sig))assert.equal(api.inspectSignature(Uint8Array.from(b),type),true);assert.equal(api.validateExtension("photo.jpg","image/jpeg"),true);assert.equal(api.validateExtension("photo.png","image/jpeg"),false)});
test("unsafe unsupported and empty files are rejected",async()=>{const fake=(name,type,data)=>({name,type,size:data.length,slice(){return{arrayBuffer:async()=>Uint8Array.from(data).buffer}}});for(const f of[fake("bad.exe","application/x-msdownload",[77,90]),fake("page.html","text/html",[60]),fake("empty.txt","text/plain",[])])assert.equal((await api.validateFile(f)).valid,false)});
test("central limits and preview limits are stable",()=>{assert.equal(api.LIMITS.image,20971520);assert.equal(api.LIMITS.pdf,52428800);assert.equal(api.LIMITS.text,5242880);assert.equal(api.isPreviewable("application/pdf",api.LIMITS.preview+1),false)});
