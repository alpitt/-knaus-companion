"use strict";const test=require("node:test"),assert=require("node:assert/strict"),o=require("../app/assets/js/workshop-owner-overlay.js");test("workshop overlay never mutates reference state",()=>{const reference={nodes:[{id:"a"}]},original=JSON.stringify(reference),overlay=o.build([{id:"WJ-OVERLAY01",status:"in-progress",affectedComponentIds:["a"],affectedSystemIds:[],defects:[]}],[],reference);assert.equal(o.getActiveJobs(overlay).length,1);overlay.reference.nodes[0].id="changed";assert.equal(JSON.stringify(reference),original)});
