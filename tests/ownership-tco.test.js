"use strict";const test=require("node:test"),assert=require("node:assert/strict"),api=require("../app/assets/js/ownership-tco.js");test("TCO keeps currencies and amount types separate",()=>{const x=api.calculate([{amount:10,currency:"EUR",amountType:"actual"},{amount:5,currency:"GBP",amountType:"estimated"},{amount:2,currency:"EUR",amountType:"refund"}]);assert.deepEqual(Object.keys(x.byCurrency).sort(),["EUR","GBP"]);assert.equal(x.byCurrency.EUR.total.actual,10);assert.equal(x.byCurrency.EUR.total.refunds,2);assert.equal(api.perDistance([],null).calculable,false)});
