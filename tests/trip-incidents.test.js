"use strict";const test=require("node:test"),assert=require("node:assert/strict"),trips=require("../app/assets/js/trips.js"),inc=require("../app/assets/js/trip-incidents.js");function setup(){let state={trips:[]};trips._reset();trips.initialise({getState:()=>state,setState:x=>state=x});return trips.create({id:"TR-INCIDENT01",title:"Trip"})}test("incident conversion is explicit and duplicate safe",()=>{const t=setup(),i=inc.create(t.id,{id:"TI-INCIDENT01",title:"Noise"});inc.convertToFault(t.id,i.id,x=>({id:`F-${x.id}`}));assert.throws(()=>inc.convertToFault(t.id,i.id,()=>({id:"F2"})),/already/)});
