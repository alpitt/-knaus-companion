"use strict";const test=require("node:test"),assert=require("node:assert/strict"),l=require("../app/assets/js/maintenance-lifecycle.js");test("maintenance lifecycle is explicit and retains reason",()=>{const x={status:"draft",revisionHistory:[]};assert.throws(()=>l.transition(x,"scheduled",""),/reason/i);assert.equal(l.transition(x,"scheduled","Approved owner schedule").revisionHistory[0].reason,"Approved owner schedule");assert.equal(l.canTransition(x,"completed"),false)});
