"use strict";const test=require("node:test"),assert=require("node:assert/strict"),o=require("../app/assets/js/maintenance-owner-overlay.js");test("maintenance overlay is derived and reference state remains unchanged",()=>{const s={maintenancePlans:[{id:"M1",componentIds:["C1"],status:"deferred"}],reference:{id:"C1"}},before=JSON.stringify(s),x=o.build(s);assert.equal(o.getComponentState(x,"C1").length,1);assert.equal(o.getDeferredItems(x).length,1);assert.equal(JSON.stringify(s),before);assert.equal(x.referenceUnchanged,true)});
