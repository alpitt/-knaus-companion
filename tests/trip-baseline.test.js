"use strict";const test=require("node:test"),assert=require("node:assert/strict"),b=require("../app/assets/js/trip-baseline.js");test("baseline retains relevant source references without mutation",()=>{const sources={faults:[{id:"F1",status:"open",confidence:"high"}],maintenance:[{id:"M1",status:"due"}],measurements:[{id:"R1",value:12.6}]},trip={unresolvedFaultIds:["F1"],relevantMaintenanceIds:["M1"]},before=JSON.stringify(sources),snap=b.createSnapshot(trip,sources);assert.equal(JSON.stringify(sources),before);assert.ok(JSON.stringify(snap).includes("F1"));assert.ok(b.compareSnapshot(snap,b.createSnapshot(trip,sources)));});
