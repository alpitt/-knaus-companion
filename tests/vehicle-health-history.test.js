"use strict";const test=require("node:test"),assert=require("node:assert/strict"),api=require("../app/assets/js/vehicle-health-history.js");test("health snapshots are explicit and never projections",()=>{let s={vehicleHealthSnapshots:[]};api._reset();api.initialise({getState:()=>s,setState:x=>s=x});const x=api.create({assessment:"attention-required",recordedAt:"2026-01-01",blockers:["B"]});assert.match(x.statement,/not roadworthiness/);assert.equal(api.compare(x.id,x.id).projection,null)});
