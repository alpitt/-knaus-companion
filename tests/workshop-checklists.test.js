"use strict";const test=require("node:test"),assert=require("node:assert/strict"),c=require("../app/assets/js/workshop-checklists.js");test("checklists require explicit status and waiver reasons",()=>{const x=c.createItem({id:"WS-TESTITEM01",title:"Record condition",phase:"inspection",blocking:true});assert.equal(c.validate(x).valid,true);assert.throws(()=>c.setStatus([x],x.id,"waived-with-reason"),/reason/);const done=c.setStatus([x],x.id,"completed");assert.equal(c.getProgress(done).percent,100);assert.equal(c.getBlockingItems(done).length,0)});
