"use strict";const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs");test("ownership routes and offline modules are integrated",()=>{const html=fs.readFileSync("app/index.html","utf8"),sw=fs.readFileSync("app/service-worker.js","utf8");for(const r of ["ownership","ownership-timeline","provenance","ownership-costs"])assert.match(html,new RegExp(`data-screen="${r}"`));assert.match(sw,/ownership-intelligence\.js/)});
