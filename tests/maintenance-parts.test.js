"use strict";const test=require("node:test"),assert=require("node:assert/strict"),p=require("../app/assets/js/maintenance-parts.js");test("part replacement retains both records and no identifiers are invented",()=>{let state={maintenanceParts:[]};p._reset();p.initialise({getState:()=>state,setState:x=>state=x});p.create({id:"MPT-OLDPART001",title:"Old"});p.create({id:"MPT-NEWPART001",title:"New"});p.replace("MPT-OLDPART001","MPT-NEWPART001",{});assert.equal(p.list().length,2);assert.equal(p.get("MPT-OLDPART001").replacementPartId,"MPT-NEWPART001");assert.equal(p.get("MPT-NEWPART001").partNumber,undefined)});
