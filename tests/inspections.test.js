"use strict";const test=require("node:test"),assert=require("node:assert/strict"),i=require("../app/assets/js/inspections.js"),templates=require("../app/data/inspection_templates.json");function setup(){let state={inspections:[]};i._reset();i.initialise({getState:()=>state,setState:x=>state=x},{templates:templates.templates})}test("inspections do not pass automatically",()=>{setup();const x=i.createFromTemplate("generic-pre-work");assert.equal(x.result,"pending");assert.equal(x.status,"draft");assert(x.checklistItems.every(y=>y.generic));assert.throws(()=>i.archive(x.id),/confirmation/)});
