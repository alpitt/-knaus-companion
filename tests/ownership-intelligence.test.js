"use strict";const test=require("node:test"),assert=require("node:assert/strict"),api=require("../app/assets/js/ownership-intelligence.js");test("ownership index is stable, traceable and keeps unknown occurrence dates",()=>{let state={faults:[{id:"F-1",title:"Recorded fault",recordedAt:"2026-01-02T00:00:00Z"}]};api._reset();api.initialise({getState:()=>state,setState:x=>state=x});const a=api.listEvents(),b=api.buildIndex();assert.deepEqual(a,b);assert.equal(a[0].sourceId,"F-1");assert.equal(a[0].occurredAt,null);assert.ok(a[0].derivationRuleId)});
