"use strict";const test=require("node:test"),assert=require("node:assert/strict"),p=require("../app/assets/js/trip-documents.js");test("document readiness uses only local user-defined requirements",()=>{const r=p.assess([{documentId:"D1"},{documentId:"D2"}],[{id:"D1",expiry:"2026-08-01"}],new Date("2026-07-28"));assert.equal(r.available.length,1);assert.equal(r.missing.length,1);assert.equal(r.expiring.length,1);assert.match(r.statement,/no legal requirement/i)});
