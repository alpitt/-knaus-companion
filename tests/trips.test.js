"use strict";const test=require("node:test"),assert=require("node:assert/strict"),lifecycle=require("../app/assets/js/trip-lifecycle.js"),baseline=require("../app/assets/js/trip-baseline.js"),readiness=require("../app/assets/js/trip-readiness.js"),checks=require("../app/assets/js/trip-checklists.js"),trips=require("../app/assets/js/trips.js");globalThis.KnausTripLifecycle=lifecycle;globalThis.KnausTripBaseline=baseline;globalThis.KnausTripReadiness=readiness;globalThis.KnausTripChecklists=checks;function setup(){let state={trips:[{id:"legacy-1",title:"Legacy touring record"}]};trips._reset();trips.initialise({getState:()=>state,setState:x=>state=x});return()=>state}test("managed trips coexist with legacy touring records and reads are immutable",()=>{const state=setup(),t=trips.create({id:"TR-TESTTRIP001",title:"Weekend",tripType:"weekend"});assert.equal(state().trips.length,2);t.title="changed";assert.equal(trips.get(t.id).title,"Weekend");assert.throws(()=>trips.create(t),/Duplicate/)});test("trip deletion and lifecycle changes require explicit action",()=>{setup();const t=trips.create({id:"TR-TESTTRIP002",title:"Trip"});assert.throws(()=>trips.remove(t.id),/confirmation/);assert.throws(()=>trips.setStatus(t.id,"planning",""),/reason/i);trips.setStatus(t.id,"planning","Planning started");assert.equal(trips.get(t.id).status,"planning")});
