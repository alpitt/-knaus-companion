"use strict";const test=require("node:test"),assert=require("node:assert/strict"),d=require("../app/assets/js/workshop-defects.js");test("defects remain separate until explicitly converted",()=>{const defect=d.create({id:"DF-TESTDEFECT1",title:"Observed damage",severity:"major"});assert.equal(defect.relatedFaultId,undefined);let created=0;const out=d.convertToFault(defect,{create:x=>(created++,{...x,id:"OR-fault"})});assert.equal(created,1);assert.equal(out.defect.relatedFaultId,"OR-fault");assert.throws(()=>d.update(defect,{status:"deferred"}),/reason/)});
