"use strict";const test=require("node:test"),assert=require("node:assert/strict"),{adapter,fresh}=require("./diagnostic-test-helper");test("resolutions stay separate from root cause and fault closure",()=>{const a=adapter({diagnosticResolutions:[]}),r=fresh("../app/assets/js/diagnostic-resolutions","KnausDiagnosticResolutions");r.initialise(a);const x=r.create({title:"Action recorded",sessionIds:["s"]});assert.equal(x.ownerConfirmed,false);assert.equal(r.reopen(x.id,"verification changed").status,"reopened")});
