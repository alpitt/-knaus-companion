"use strict";const test=require("node:test"),assert=require("node:assert/strict"),baseline=require("../app/assets/js/change-baseline.js");
test("baseline captures only relevant entities without mutating reference data",()=>{const node={id:"component-a",name:"A",confidence:"confirmed",provenance:"manual"},graph={getNode:id=>id===node.id?node:null,getEdges:()=>[]},snapshot=baseline.createSnapshot({affectedComponentIds:["component-a","missing"]},{graph,twin:{getComponents:()=>[node]},records:{list:()=>[]},evidence:{list:()=>[]}});assert.equal(snapshot.nodes.length,1);assert.equal(snapshot.unknowns.length,1);snapshot.nodes[0].name="changed";assert.equal(node.name,"A");assert.equal(baseline.validateSnapshot(snapshot).valid,true);assert.equal(baseline.getSnapshotSummary(snapshot).complete,false)});
