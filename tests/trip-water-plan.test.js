"use strict";const test=require("node:test"),assert=require("node:assert/strict"),p=require("../app/assets/js/trip-water-plan.js");test("water plan uses explicit capacity and consumption only",()=>{assert.equal(p.calculateEstimatedDuration({capacity:100,departurePercent:50,dailyUse:25}),2);assert.equal(p.calculatePlannedQuantity({departurePercent:50}),null);assert.ok(p.assess({knownLeaks:["leak"]}).warnings.length)});
