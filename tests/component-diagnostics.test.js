"use strict";const test=require("node:test"),assert=require("node:assert/strict"),{fresh}=require("./diagnostic-test-helper");test("component diagnostics are a read-only non-reliability overlay",()=>{const x=fresh("../app/assets/js/component-diagnostics","KnausComponentDiagnostics"),source={sessions:[{id:"s",componentIds:["c"],status:"paused"}]};x.build(source);source.sessions[0].status="changed";assert.equal(x.getActiveSessions("c")[0].status,"paused");assert.equal(x.getQualityReport("c").limitation.includes("not reliability"),true)});
