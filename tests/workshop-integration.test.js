"use strict";const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),root=path.resolve(__dirname,"..");test("workshop routes storage backup and offline assets are additive",()=>{const html=fs.readFileSync(path.join(root,"app/index.html"),"utf8"),app=fs.readFileSync(path.join(root,"app/assets/js/app-v4.js"),"utf8"),worker=fs.readFileSync(path.join(root,"app/service-worker.js"),"utf8");for(const route of["workshop-jobs","workshop-job","inspections"])assert.match(html,new RegExp(`data-screen="${route}"`));assert.match(app,/workshopJobs:\[\],inspections:\[\]/);assert.match(app,/const STORE_KEY="knaus-ultimate-v1"/);for(const asset of["workshop_job.schema.json","inspection.schema.json","workshop-jobs.js","inspections.js","workshop-owner-overlay.js"])assert.match(worker,new RegExp(asset.replace(".","\\.")));assert.doesNotMatch(worker,/workshopJobs\.json/)});
