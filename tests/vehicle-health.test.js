"use strict";const test=require("node:test"),assert=require("node:assert/strict"),v=require("../app/assets/js/vehicle-health.js"),rules=require("../app/data/vehicle_health_rules.json");test("vehicle health exposes blockers and is not roadworthiness",()=>{const x=v.assess({faults:[{severity:"critical"}],maintenance:[],deferredMaintenance:[]},rules);assert.equal(x.result,"critical-attention");assert.ok(x.score<100);assert.match(x.statement,/not roadworthiness/i);assert.equal("probability" in x,false)});
