"use strict";const test=require("node:test"),assert=require("node:assert/strict"),api=require("../app/assets/js/system-provenance.js");test("system aggregation deduplicates shared records",()=>{const e={id:"E",sourceType:"fault",eventType:"fault-opened",systemIds:["S"]};api.initialise({events:[e,e]});assert.equal(api.getSystemHistory("S").length,1);assert.match(api.build("S").statement,/does not establish causation/)});
