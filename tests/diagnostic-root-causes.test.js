"use strict";const test=require("node:test"),assert=require("node:assert/strict"),{adapter,fresh}=require("./diagnostic-test-helper");test("root causes require explicit confirmation",()=>{const a=adapter({diagnosticRootCauses:[]}),r=fresh("../app/assets/js/diagnostic-root-causes","KnausDiagnosticRootCauses");r.initialise(a);const x=r.createProposal({title:"Owner proposal",sessionIds:["s"]});assert.equal(x.status,"proposed");assert.throws(()=>r.confirmOwnerRecord(x.id,""));assert.equal(r.confirmOwnerRecord(x.id,"confirmed by owner").status,"owner-confirmed")});
