"use strict";const test=require("node:test"),assert=require("node:assert/strict"),r=require("../app/assets/js/trip-readiness.js");test("readiness is deterministic, conservative and does not mutate",()=>{const t={unresolvedFaultIds:["F1"],departureChecklist:[],acknowledgements:["noted"]},before=JSON.stringify(t),a=r.assess(t,{faults:[{id:"F1",title:"Brake concern",severity:"critical"}]});assert.equal(a.state,"blocked");assert.equal(a.categories.length,24);assert.equal(JSON.stringify(t),before);assert.ok(a.blockers.some(x=>/Brake/.test(x)))});
