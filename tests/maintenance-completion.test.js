"use strict";const test=require("node:test"),assert=require("node:assert/strict"),c=require("../app/assets/js/maintenance-completion.js");test("completion is explicit and partial work remains visible",()=>{const m={id:"MP-COMPLETE01",completionHistory:[]};assert.throws(()=>c.complete(m,{completedAt:"2026-01-01"}),/confirmation/i);const x=c.partiallyComplete(m,{confirmed:true,completedAt:"2026-01-01",incompleteWork:["Evidence"]});assert.equal(x.status,"partially-completed");assert.deepEqual(c.getIncomplete(x.lastCompleted),["Evidence"])});
