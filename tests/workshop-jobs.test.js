"use strict";const test=require("node:test"),assert=require("node:assert/strict"),jobs=require("../app/assets/js/workshop-jobs.js");function setup(){let state={workshopJobs:[]};jobs._reset();jobs.initialise({getState:()=>state,setState:x=>state=x})}test("workshop jobs use explicit immutable local lifecycle",()=>{setup();const j=jobs.create({id:"WJ-TESTJOB001",title:"Test job",jobType:"repair",jobScope:"Defined repair"});assert.equal(j.status,"draft");assert.throws(()=>jobs.create(j),/Duplicate/);jobs.update(j.id,{description:"updated"});const read=jobs.get(j.id);read.title="changed";assert.notEqual(jobs.get(j.id).title,"changed");assert.throws(()=>jobs.archive(j.id),/confirmation/);jobs.archive(j.id,true);jobs.restore(j.id);assert.throws(()=>jobs.remove(j.id),/confirmation/);assert.equal(jobs.remove(j.id,true),true)});test("source conversions are explicit and duplicate-safe",()=>{setup();const f={id:"fault-1",title:"Water leak",componentIds:["pump"]};const j=jobs.createFromFault(f);assert.deepEqual(j.relatedFaultIds,[f.id]);assert.throws(()=>jobs.createFromFault(f),/already exists/)});
