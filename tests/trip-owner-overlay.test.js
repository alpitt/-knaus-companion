"use strict";const test=require("node:test"),assert=require("node:assert/strict"),trips=require("../app/assets/js/trips.js"),overlay=require("../app/assets/js/trip-owner-overlay.js");test("owner overlay derives state without replacing reference data",()=>{let state={trips:[]};trips._reset();trips.initialise({getState:()=>state,setState:x=>state=x});trips.create({id:"TR-OVERLAY01",title:"Trip",affectedComponentIds:["battery"]});assert.equal(overlay.getComponentTripState("battery").length,1);assert.equal(overlay.getQualityReport().missingBaselines.length,1)});
