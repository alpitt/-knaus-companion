"use strict";const test=require("node:test"),assert=require("node:assert/strict"),p=require("../app/assets/js/trip-gas-plan.js");test("gas plan requires entered tare and measured weights",()=>{assert.equal(p.calculateGasRemaining({tareWeight:8,measuredWeight:13}),5);assert.equal(p.calculateGasRemaining({tareWeight:13,measuredWeight:8}),null);assert.ok(p.assess({}).unknowns.length)});
