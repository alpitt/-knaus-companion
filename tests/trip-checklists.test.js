"use strict";const test=require("node:test"),assert=require("node:assert/strict"),c=require("../app/assets/js/trip-checklists.js");test("required skip and waiver retain a reason",()=>{const x=c.createItem({id:"C-1",phase:"pre-departure",title:"Door",required:true});assert.throws(()=>c.setStatus([x],x.id,"skipped"),/reason/i);assert.equal(c.setStatus([x],x.id,"waived-with-reason","Not fitted")[0].status,"waived-with-reason")});
