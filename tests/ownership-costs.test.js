"use strict";const test=require("node:test"),assert=require("node:assert/strict"),api=require("../app/assets/js/ownership-costs.js");test("explicit costs persist and over-allocation is rejected",()=>{let s={ownershipCosts:[]};api._reset();api.initialise({getState:()=>s,setState:x=>s=x});api.create({id:"OC-TEST001",title:"Part",amount:10,currency:"EUR"});assert.equal(s.ownershipCosts.length,1);assert.throws(()=>api.allocate("OC-TEST001",[{targetType:"system",targetId:"electrical",amount:11}]),/cannot exceed/)});
