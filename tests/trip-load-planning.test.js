"use strict";const test=require("node:test"),assert=require("node:assert/strict"),p=require("../app/assets/js/trip-load-planning.js");test("load planning preserves unknowns and infers no limits",()=>{const r=p.calculate({items:[{category:"bicycle",quantity:2,weightPerItem:22,weightUnit:"lb"},{category:"passenger"}],removedWeightKg:1});assert.ok(r.netLoadKg>18&&r.netLoadKg<20);assert.equal(r.unknownItems.length,1);assert.equal(r.remainingPayloadKg,null);assert.match(r.trace[0].formula,/entered weight/)});
