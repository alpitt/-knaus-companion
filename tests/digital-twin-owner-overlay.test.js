const test=require("node:test"),assert=require("node:assert/strict"),overlay=require("../app/assets/js/digital-twin-owner-overlay.js"),twin=require("../app/data/digital_twin.json");
test("overlay reports modifications, faults and measurements without mutating Twin",()=>{const before=structuredClone(twin),component=twin.components[0].id,records=[{id:"OR-OVERLAY001",type:"modification",title:"Fixture modification",status:"completed",componentIds:[component],systemId:twin.systems[0].id,occurredAt:"2026-01-01T00:00:00Z"},{id:"OR-OVERLAY002",type:"fault",title:"Fixture fault",status:"open",componentIds:[component],occurredAt:"2026-02-01T00:00:00Z"},{id:"OR-OVERLAY003",type:"measurement",title:"Fixture measurement",status:"open",componentIds:[component],occurredAt:"2026-03-01T00:00:00Z",measurements:[{value:12.7,unit:"V"}]}];overlay.build(twin,records,[]);const state=overlay.getComponentState(component);assert.equal(state.completedModifications,1);assert.equal(state.openFaults,1);assert.equal(state.latestMeasurement.id,"OR-OVERLAY003");assert.deepEqual(twin,before);assert.equal(overlay.getQualityReport().referenceMutated,false)});
test("overlay reports broken references and reversed modifications are historical",()=>{overlay.build(twin,[{id:"OR-BROKEN0001",type:"modification",status:"reversed",componentIds:["missing"],systemId:"missing"}],[]);const quality=overlay.getQualityReport();assert.equal(quality.brokenComponentReferences.length,1);assert.equal(quality.brokenSystemReferences.length,1);assert.equal(overlay.getModificationState()[0].status,"reversed")});
