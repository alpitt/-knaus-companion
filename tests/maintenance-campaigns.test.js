"use strict";const test=require("node:test"),assert=require("node:assert/strict"),c=require("../app/assets/js/maintenance-campaigns.js");test("campaigns retain incomplete child tasks",()=>{let state={maintenanceCampaigns:[]};c._reset();c.initialise({getState:()=>state,setState:x=>state=x});const x=c.create({id:"MC-CAMPAIGN01",title:"Review",tasks:[{id:"T1",status:"completed"},{id:"T2",status:"draft",blocking:true}]});assert.deepEqual(c.getProgress(x.id),{total:2,completed:1});assert.equal(c.getBlockingItems(x.id).length,1)});
