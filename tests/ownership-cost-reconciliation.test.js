"use strict";const test=require("node:test"),assert=require("node:assert/strict"),api=require("../app/assets/js/ownership-cost-reconciliation.js");test("source costs are preview candidates and never auto-created",()=>{const c=api.scan({workshopJobs:[{id:"WJ-1",title:"Repair",actualCost:20,currency:"EUR"}]},[])[0];assert.equal(c.previewOnly,true);assert.throws(()=>api.createExplicit(c,{create:()=>{}},false),/confirmation/)});
