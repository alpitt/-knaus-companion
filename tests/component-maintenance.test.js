"use strict";const test=require("node:test"),assert=require("node:assert/strict"),c=require("../app/assets/js/component-maintenance.js");test("component profile preserves reference input and reports missing identity",()=>{const s={components:[{id:"C1",name:"Reference"}],maintenancePlans:[{id:"M1",componentIds:["C1"],completionHistory:[]}]},before=JSON.stringify(s),p=c.buildProfile("C1",s);assert.equal(p.maintenancePlans.length,1);assert.equal(JSON.stringify(s),before);assert.equal(c.getQualityReport(p).brokenComponentReference,false);assert.equal(c.getQualityReport(c.buildProfile("missing",s)).brokenComponentReference,true)});
