"use strict";const test=require("node:test"),assert=require("node:assert/strict"),life=require("../app/assets/js/trip-lifecycle.js");test("trip lifecycle rejects hidden advancement and blocked departure",()=>{const t={status:"draft",blockers:[],revisionHistory:[]};assert.throws(()=>life.transition(t,"planning",""),/reason/i);assert.equal(life.transition(t,"planning","Begin planning").status,"planning");assert.equal(life.canTransition({...t,status:"ready-for-review",blockers:["fault"]},"ready-to-depart"),false)});
