"use strict";const fs=require("node:fs"),path=require("node:path");const root=path.resolve(__dirname,"..");for(const f of ["ownership_profile.schema.json","ownership_event.schema.json","vehicle_health_snapshot.schema.json"]){const p=path.join(root,"app/data",f);if(!fs.existsSync(p))throw Error(`Missing ${p}`);const x=JSON.parse(fs.readFileSync(p,"utf8"));if(x.$schema!=="https://json-schema.org/draft/2020-12/schema")throw Error(`${f} is not JSON Schema draft 2020-12`)}console.log("Ownership contracts valid.");
