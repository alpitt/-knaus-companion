"use strict";const v=require("./procedure-validation-common");v.validateSchemas();const t=v.json("app/data/procedure_templates.json");if(!Array.isArray(t.templates))throw Error("procedure_templates.json templates must be an array");for(const x of t.templates)if(!x.generic)throw Error(`Template ${x.id} is not labelled generic`);console.log(`Procedure contracts valid; ${t.templates.length} generic structure template(s), no technical procedure content.`);
