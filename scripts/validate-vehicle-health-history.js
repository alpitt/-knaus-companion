"use strict";const fs=require("node:fs"),path=require("node:path"),root=path.resolve(__dirname,"..");const schema=JSON.parse(fs.readFileSync(path.join(root,"app/data/vehicle_health_snapshot.schema.json"),"utf8"));if(!schema.properties?.overallResult||!schema.properties?.recordedAt)throw Error("Vehicle Health snapshot contract incomplete");if(!fs.readFileSync(path.join(root,"app/assets/js/vehicle-health-history.js"),"utf8").includes("not roadworthiness"))throw Error("Roadworthiness limitation missing");console.log("Vehicle Health history contract valid.");
