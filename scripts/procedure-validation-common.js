"use strict";const fs=require("node:fs"),path=require("node:path"),root=path.resolve(__dirname,"..");function json(rel){return JSON.parse(fs.readFileSync(path.join(root,rel),"utf8"))}function requireFiles(files){const missing=files.filter(x=>!fs.existsSync(path.join(root,x)));if(missing.length)throw Error(`Missing required files: ${missing.join(", ")}`)}function validateSchemas(){const names=["procedure","procedure_step","procedure_requirement","procedure_template","procedure_execution","procedure_deviation","lesson_learned","procedure_revision_proposal","technical_library_item","knowledge_relationship"];for(const n of names){const x=json(`app/data/${n}.schema.json`);if(x.$schema!=="https://json-schema.org/draft/2020-12/schema")throw Error(`${n}.schema.json is not JSON Schema 2020-12`);if(!x.type)throw Error(`${n}.schema.json has no root type`)}}module.exports={root,json,requireFiles,validateSchemas};
