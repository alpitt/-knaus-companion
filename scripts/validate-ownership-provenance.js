"use strict";const fs=require("node:fs"),path=require("node:path"),root=path.resolve(__dirname,"..");for(const f of ["ownership-provenance.js","component-provenance.js","system-provenance.js"]){const p=path.join(root,"app/assets/js",f);if(!fs.existsSync(p)||!fs.readFileSync(p,"utf8").includes("Provenance"))throw Error(`Invalid provenance module ${f}`)}console.log("Ownership provenance modules valid.");
