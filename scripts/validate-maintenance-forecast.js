"use strict";const fs=require("node:fs"),p=require("node:path"),root=p.resolve(__dirname,"..");function main(){const x=JSON.parse(fs.readFileSync(p.join(root,"app/data/maintenance_forecast.schema.json"),"utf8"));if(x.$schema!=="https://json-schema.org/draft/2020-12/schema")throw Error("Forecast schema invalid");console.log("Maintenance forecast validation passed.")}if(require.main===module)main();module.exports={main};
