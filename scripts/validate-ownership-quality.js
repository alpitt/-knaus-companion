"use strict";const fs=require("node:fs"),path=require("node:path"),root=path.resolve(__dirname,"..");const rules=JSON.parse(fs.readFileSync(path.join(root,"app/data/ownership_quality_rules.json"),"utf8"));if(!Array.isArray(rules.rules)||!rules.rules.length)throw Error("Ownership quality rules are empty");if(!fs.readFileSync(path.join(root,"app/assets/js/ownership-quality.js"),"utf8").includes("automatically repair"))throw Error("Non-destructive quality rule missing");console.log("Ownership quality rules valid.");
