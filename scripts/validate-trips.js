"use strict";const fs=require("node:fs"),path=require("node:path"),root=path.resolve(__dirname,".."),trips=require("../app/assets/js/trips.js");function main(){const schema=JSON.parse(fs.readFileSync(path.join(root,"app/data/trip.schema.json"),"utf8"));if(schema.$schema!=="https://json-schema.org/draft/2020-12/schema")throw Error("Trip schema must use JSON Schema draft 2020-12.");if(!schema.required?.includes("id")||!schema.required?.includes("title"))throw Error("Trip schema required fields are incomplete.");for(const file of fs.readdirSync(path.join(root,"tests/fixtures/trips")).filter(x=>x.endsWith(".json"))){const fixture=JSON.parse(fs.readFileSync(path.join(root,"tests/fixtures/trips",file),"utf8")),result=trips.validate(fixture.trip||fixture);if(fixture.expectedValid&&!result.valid)throw Error(`${file}: ${result.errors.join(" ")}`);if(fixture.expectedValid===false&&result.valid)throw Error(`${file}: expected invalid trip`)}console.log("Trip validation passed.")}if(require.main===module){try{main()}catch(e){console.error(`Trip validation failed: ${e.message}`);process.exitCode=1}}module.exports={main};
